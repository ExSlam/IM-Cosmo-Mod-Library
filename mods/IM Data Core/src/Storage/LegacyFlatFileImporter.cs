namespace IMDataCore
{
    // Pre-2.0 flat-file JSON migration support was removed intentionally.
    // IM Data Core 2.x runtime persistence uses only the lightweight JSON sidecar.
    // Historical database migration, if provided, belongs in a separate migration tool.
}
